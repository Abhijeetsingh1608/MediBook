package com.medibook.appointment.entity;

/*
 * This enum stores fixed constant values for AppointmentStatus.
 * It helps avoid using random string values in the project.
 */
public enum AppointmentStatus {
    BOOKED,
    CANCELLED,
    COMPLETED
}
