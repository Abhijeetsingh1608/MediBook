package com.medibook.notification.entity;

/*
 * This enum stores fixed constant values for NotificationStatus.
 * It helps avoid using random string values in the project.
 */
public enum NotificationStatus {
    SENT,
    FAILED
}
