package com.medibook.auth.entity;

/*
 * This enum stores fixed constant values for UserRole.
 * It helps avoid using random string values in the project.
 */
public enum UserRole {
    PATIENT,
    PROVIDER,
    ADMIN
}
