package com.medibook.schedule.entity;

/*
 * This enum stores fixed constant values for SlotStatus.
 * It helps avoid using random string values in the project.
 */
public enum SlotStatus {
    AVAILABLE,
    BOOKED,
    CANCELLED
}
